package ironflow

// SDKVersion is the Go SDK version reported to the server on worker
// registration. Kept in sync with the JS SDK and root VERSION by scripts/release.sh.
const SDKVersion = "0.23.0"
